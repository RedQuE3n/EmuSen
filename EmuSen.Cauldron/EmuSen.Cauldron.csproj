<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- No longer packed, and the reason it was is worth keeping. EmuSen.LunaP
       named this as a dependency and is packed, so a consumer outside this
       repository could not have resolved a ProjectReference to it - which made
       this a package too, and made anyone consuming the toolkit carry it. LunaP
       stopped naming anything of EmuSen's, so nothing outside this repository
       wants this assembly and there is nothing for a package to be for. The
       metadata is kept rather than deleted: if a consumer does turn up, the id
       and the licence are already decided. See EmuSen_LunaP.md §19 and §20. -->
  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <PackageId>EmuSen.Cauldron</PackageId>
    <Version>0.1.0</Version>
    <Authors>Tristan Pelland</Authors>
    <Description>Read-only telemetry contracts shared by EmuSen frontends.</Description>
    <PackageLicenseExpression>GPL-3.0-or-later</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/RedQuE3n/EmuSen-Project</RepositoryUrl>
  </PropertyGroup>

</Project>

<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="EmuSen.Hotaru.Views.CoretopWindow"
        Title="DianaOS coretop"
        Width="480" Height="720"
        MinWidth="360" MinHeight="360"
        Background="#1E1E1E">

    <ScrollViewer>
        <StackPanel Margin="12" Spacing="8">
            <TextBlock x:Name="HeaderText"
                       Foreground="#D4D4D4"
                       FontFamily="Consolas,Menlo,monospace"
                       FontWeight="Bold"
                       FontSize="14" />
            <TextBlock x:Name="NoTargetText"
                       Text="No ROM loaded."
                       Foreground="Gray"
                       IsVisible="False" />

            <TextBlock Text="Hardware load" Foreground="#9CDCFE" FontWeight="Bold" />
            <StackPanel x:Name="LoadPanel" Spacing="3" />

            <TextBlock Text="CPU registers" Foreground="#9CDCFE" FontWeight="Bold" />
            <TextBlock x:Name="CpuRegsText"
                       Foreground="#D4D4D4"
                       FontFamily="Consolas,Menlo,monospace"
                       TextWrapping="Wrap" />

            <TextBlock Text="Sprites" Foreground="#9CDCFE" FontWeight="Bold" />
            <Grid ColumnDefinitions="*,Auto">
                <ProgressBar x:Name="SpritesBar" Grid.Column="0" Minimum="0" Maximum="100" Height="16" />
                <TextBlock x:Name="SpritesText"
                           Grid.Column="1"
                           Margin="8,0,0,0"
                           Foreground="#D4D4D4"
                           VerticalAlignment="Center" />
            </Grid>

            <TextBlock Text="Audio channels" Foreground="#9CDCFE" FontWeight="Bold" />
            <StackPanel x:Name="AudioPanel" Spacing="3" />

            <TextBlock Text="Palette (color RAM)" Foreground="#9CDCFE" FontWeight="Bold" />
            <Image x:Name="PaletteImage"
                   Stretch="None"
                   RenderOptions.BitmapInterpolationMode="None"
                   HorizontalAlignment="Left" />

            <TextBlock Text="VRAM tile sheet" Foreground="#9CDCFE" FontWeight="Bold" />
            <Image x:Name="TileSheetImage"
                   Stretch="Uniform"
                   MaxHeight="320"
                   RenderOptions.BitmapInterpolationMode="None"
                   HorizontalAlignment="Left" />
        </StackPanel>
    </ScrollViewer>

</Window>

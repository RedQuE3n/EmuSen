# .NET Build Outputs
bin/
obj/
TestResults/

# OS/IDE generated files (Standard additions)
.DS_Store
.vs/
.vscode/

# DianaOS's own Unix-shaped runtime tree (var/log, var/games, var/lib,
# home/<user>, etc, tmp) - see `man hier` - real, uncommitted data, not
# source. *.srm/*.state kept for anything still written outside it
# (EmuSen.Mistress9's own AppData-based save path).
var/
home/
etc/
tmp/
*.srm
*.state

# Copyrighted commercial ROMs dropped here for local debugging/testing -
# never belongs in version control (redistribution), regardless of who
# added it or why.
TestRoms/
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The core-agnostic ISingleStepTarget/SingleStepTestRunner (EmuSen/
         Validation/*) plus each core's own adapter (e.g.
         Cores/Nintendo/Venus - SNES/Validation/*) - this project is just
         the thin CLI wrapper that picks a target by name and runs it. -->
    <ProjectReference Include="..\EmuSen\EmuSen.csproj" />
  </ItemGroup>

</Project>
